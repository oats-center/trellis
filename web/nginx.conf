server {
  listen 80;
  listen [::]:80;
  server_name _;

  root /usr/share/nginx/html;
  index 200.html;

  location / {
    try_files $uri $uri/ /200.html;
  }

  location /healthz {
    access_log off;
    default_type text/plain;
    return 200 'ok';
  }
}

//! Live acceptance tests for the external Rust testkit.
//!
//! These tests are consciously selected by the fixture's `live` target and run
//! against real `trellis`/`trellis-server` executables; they never start
//! infrastructure during compilation or documentation.

#[test]
fn placeholder_until_generated_identifiers_are_recorded() {}
